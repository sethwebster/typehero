export const DEMO_CODE = `function calculateSpeed(wpm, accuracy) {
  if (accuracy < 90) return "Practice more!";
  return \`You are typing at \${wpm} WPM!\`;
}

// Start your journey today
const hero = new TypeHero();
hero.train();`;
