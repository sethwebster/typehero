import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TypeHero | Master the Keyboard",
  description: "The terminal command that makes you a better typist. Practice with real code, track your progress, and master your keyboard.",
  keywords: ["typing", "terminal", "cli", "developer tools", "keyboard practice", "touch typing"],
  openGraph: {
    title: "TypeHero | Master the Keyboard",
    description: "The terminal command that makes you a better typist.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
